# .env.prod.example  (production template)
# Copy to a real ".env" on the server and fill in real values.
# Do NOT commit your real .env with secrets.

# ── Core ───────────────────────────────────────────────────────────────────────
# Point Django at the production settings module.
DJANGO_SETTINGS_MODULE=languagelink.settings.prod

# REQUIRED: strong, private secret key. Generate with:
#   python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=REPLACE_WITH_STRONG_RANDOM_VALUE

# Your public site URL used in emails and links.
SITE_URL=https://advising.langcen.cam.ac.uk

# Hosts Django is allowed to serve (comma-separated).
# Include every hostname you’ll use, with or without www.
ALLOWED_HOSTS=advising.langcen.cam.ac.uk

# CSRF trusted origins (comma-separated). MUST include scheme (https://).
# Include each origin from which you’ll submit forms (usually your site).
CSRF_TRUSTED_ORIGINS=https://advising.langcen.cam.ac.uk

# Optional: adjust password reset link expiry (seconds). Default ~72h.
#PASSWORD_RESET_TIMEOUT=259200


# ── Database (MariaDB/MySQL) ───────────────────────────────────────────────────
# These are used by languagelink.settings.prod (ENGINE=mysql).
DB_NAME=languagelink
DB_USER=languagelink_user
DB_PASSWORD=REPLACE_WITH_DB_PASSWORD
DB_HOST=127.0.0.1
DB_PORT=3306
# Note: prod.py already sets utf8mb4 + strict SQL mode.


# ── Email (SMTP) ───────────────────────────────────────────────────────────────
# Use your university SMTP here.
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend

# From address that recipients will see
DEFAULT_FROM_EMAIL=LanguageLink <no-reply@langcen.cam.ac.uk>

# SMTP connection details
EMAIL_HOST=smtp.university.edu
EMAIL_PORT=587
EMAIL_HOST_USER=service_account_username
EMAIL_HOST_PASSWORD=REPLACE_WITH_SMTP_PASSWORD

# TLS/SSL flags (set exactly one of these to true in most setups; TLS is common)
EMAIL_USE_TLS=true
EMAIL_USE_SSL=false

# Timeout in seconds for SMTP operations
EMAIL_TIMEOUT=10

# Optional subject prefix to group app emails in inboxes
EMAIL_SUBJECT_PREFIX=[LanguageLink]


# ── Security (prod.py has sensible defaults) ───────────────────────────────────
# prod.py sets:
#   DEBUG = False
#   SESSION_COOKIE_SECURE = True
#   CSRF_COOKIE_SECURE = True
#   SECURE_PROXY_SSL_HEADER = ("HTTP_X_FORWARDED_PROTO", "https")
#   SECURE_SSL_REDIRECT = True
# If you terminate TLS at a proxy, ensure it sets X-Forwarded-Proto: https.
# If you need to toggle SSL redirect via env, add a flag and read it in prod.py.


# ── Static/Media notes (not env values) ────────────────────────────────────────
# prod.py sets STATIC_ROOT to <BASE_DIR>/staticfiles for collectstatic.
# Configure your web server or proxy to serve /static/ from that path.
# Media uploads live under MEDIA_ROOT (see settings/base.py); serve via Nginx or S3.
