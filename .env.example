# .env.example  (development template)
# Copy to ".env" for local development, then adjust values.
# This file is safe to commit (no secrets).

# Optional: makes shell scripts/pylance pick the right settings without manage.py
#DJANGO_SETTINGS_MODULE=languagelink.settings.dev

# --- Django ---
# Generate your own dev key (OK to be non-secret for local work)
# e.g. python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=dev-not-secret-change-me

# Where links in emails should point during development
SITE_URL=http://localhost:8000

# Optional: override (seconds). Django default ~72h.
#PASSWORD_RESET_TIMEOUT=259200

# --- Email (dev) ---
# File-based email backend writes .eml files to disk so nothing is actually sent.
EMAIL_BACKEND=django.core.mail.backends.filebased.EmailBackend
EMAIL_FILE_PATH=./tmp_emails

# From address + subject prefix for app emails
DEFAULT_FROM_EMAIL=LanguageLink <no-reply@langcen.cam.ac.uk>
EMAIL_SUBJECT_PREFIX=[LanguageLink]

# Optional email tuning
EMAIL_TIMEOUT=10
#EMAIL_USE_TLS=true
#EMAIL_USE_SSL=false

# --- Misc app settings (optional) ---
#BOOKING_LEAD_MINUTES=60
